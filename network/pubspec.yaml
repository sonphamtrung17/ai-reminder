name: network
description: "A new Flutter project."
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  shared:
    path: ../shared
  domain:
    path: ../domain

  dio:
  dartx:
  tuple:

  #Dependency Injection
  get_it:
  injectable:

  freezed_annotation:
  json_annotation:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

  build_runner:
  injectable_generator:

  #Model
  freezed:
  json_serializable:
