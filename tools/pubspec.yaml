name: gen_env
description: Generate config docs for VSCode & Android Studio
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.8.1

dependencies:
  yaml: ^3.1.3
  xml: ^6.6.1
  collection: ^1.19.1