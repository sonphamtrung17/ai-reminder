name: shared
description: "Module shared"
version: 0.0.1
homepage:
resolution: workspace

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  device_info_plus: ^11.5.0
  flutter_udid: ^4.0.0
  android_id: ^0.4.0
  dartx: ^1.2.0
  package_info_plus: ^8.3.1
  async: ^2.13.0

  #Dependency Injection
  get_it: ^8.2.0
  injectable: ^2.5.1

  shared_preferences: ^2.5.3
  flutter_secure_storage: ^9.2.4

  freezed_annotation: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  build_runner: ^2.7.0
  injectable_generator: ^2.8.1
  freezed: ^3.2.0